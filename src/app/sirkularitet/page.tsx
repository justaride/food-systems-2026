export const metadata = { title: 'Sirkularitet — Food Systems 2026' }

import { SirkularitetContent } from './SirkularitetContent'

export default function SirkularitetPage() {
  return <SirkularitetContent />
}
