# AP-5 LØST — status 2026-08-05

**Utført av:** agent (Kimi), sesjon 2026-08-05 kveld
**Gren/worktree:** `codex/nordic-knowledge-canonical-v1` (`.worktrees/nordic-knowledge-canonical-v1`)
**Commits laget:** ingen. **Databaseskriving:** ingen. **`--apply`:** ikke kjørt.
**Sporede repo-filer endret:** ingen.

---

## 1. Kort konklusjon

AP-5 i sin opprinnelige betydning — den formålsbundne FD3/FD4/FD5-startmekanismen — var **allerede løst** i sesjon 2 (S2-C) og ligger integrert på den kanoniske grenen som commit `e8ad258` + `4f38830`. Karantenen er intakt: `--apply` feiler fortsatt med `APPLY_TRUSTED_ENTRYPOINT_REQUIRED`, slik den skal.

Den faktiske gjenstående blokkeringen (fra AP-7) var den **utdaterte manifest-pinnen** i den låste planen. Den er nå verifisert løst på plannivå: S3-Cs kandidatplan er reprodusert mot live tilstand og er **byte-identisk med en fersk dry-run** (unntatt tidsstempelavledede felt). Det som gjenstår er eiergodkjenning og en kontrollert plan-/kontraktsrevisjon — ikke teknisk arbeid.

Underveis ble det funnet og reparert en **skadet ekstern runtime-tre** (ny F9-variant), og avdekket **database-drift utenfor den revisjonerte porten** (nytt funn, se §5).

## 2. Rotårsaker

1. **Stale manifest-pin (AP-7s blokkering).** Den låste planen `source-registration-dry-run-plan-2026-08-03.v1.json` binder manifestfilhash `897f3599…`. Commit `006986f` (3. august, altså eldre enn nattsesjonen) endret manifestet til `631ad900…`. Apply-verktøyet stopper fail-closed på dette — korrekt oppførsel.
2. **Skadet runtime-tre (ny F9-variant).** Den eksterne runtime-roten `/private/tmp/food-systems-runtime.uecW2d` (opprettet av AP-7) var delvis ødelagt: 123 av 539 toppnivåpakker manglet `package.json` (bl.a. `tsx`, `typescript`, `next`), og den genererte Prisma-klienten var redusert til 7 filer. Hele kontrollpakken feilet med `ERR_MODULE_NOT_FOUND`. Årsak til skaden er ukjent (selektiv filsletting i `/tmp`); treet var ubrukelig og er forlatt.
3. **Database-drift utenfor porten (nytt funn).** `SourceDoc` er vokst 199 → 254 og `SourceCitation` 2703 → 2794. De 91 nye sitatradene har `createdAt` 2026-08-05 ~15:00 UTC — altså *etter* AP-7s grønne kjøring 4. august. `Document` (1539), `LibraryAnalysisRecord` (1572) og `ControlledMutationAudit` (0) er uendret, og databasesystemidentifikator er den sealede `7620055716543368057`. Skrivingen skjedde altså utenfor den revisjonerte registreringsporten, men den gjør den immutable health-snapshoten `health.snapshot_set.2026-08-03.006986f7` og apply-kontraktens binding til «555 189 rader over 56 relasjoner» foreldet. **Hvem/hva som skrev disse radene må identifiseres og godkjennes av eier.**

## 3. Hva som ble gjort

### 3.1 Verifisert at AP-5-mekanismen er på plass og karantenen holder

- `env -i … node scripts/knowledge/launch-locked-source-registration-apply.mjs --apply` → `APPLY_TRUSTED_ENTRYPOINT_REQUIRED`, exit 1. Uendret, som kontrakten krever.
- `node --import=tsx --test tests/lib/source-registration-trusted-entrypoint.test.ts` → **9/9 pass**, inkl. «FD5 leses ikke ved tidlig feil», «direkte kjøring av barn avvises» og «drift etter barn avvises».

### 3.2 Reparert runtime (F9-varianten) uten å røre sporede filer

- Bygget nytt tre i staging: `npm ci --no-audit --no-fund` (824 pakker, rent) + `prisma generate` (Prisma Client 7.7.0) i `/private/tmp/food-systems-runtime-ap5.HfN1fe`, fra worktreets `package.json` + `package-lock.json`.
- Byttet symlinkene `node_modules` og `src/generated/prisma` i worktreet til det nye treet (samme eksterne/symlinkede layout som AP-7 etablerte). Ingen sporede filer endret; `package-lock.json` urørt.
- Det gamle, skadede treet ligger igjen på `/private/tmp/food-systems-runtime.uecW2d` — kan slettes etter gjennomgang.

### 3.3 Verifisert kandidatplanen mot live tilstand (read-only)

- Kjørte generatoren på nytt i dry-run med kandidatbatchen og private røtter fra `.env.local`:
  `generate-source-registration-plan.ts --dry-run --manifest=…-batch-2026-08-04-candidate.v1.json --primary-corpus-root=<privat-rot> --replica-corpus-root=<privat-rot>`
- Resultat: `targetCount:10, pending:10, alreadyRegistered:0, conflicts:0, privateRecoveryAppend:10, 558 sider, 213325 ord`, `applyEnabled:false, databaseMutationPerformed:false, networkAccessPerformed:false`.
- Diffet fersk plan mot `source-registration-candidate-plan-2026-08-04.v1.json` med volatile felt maskert (`observedAt`, `planSha256`, `beforeSnapshotSha256`, samt `createdAt`/`updatedAt` lik plan-`observedAt`): **identiske**. Kandidaten beskriver altså nøyaktig dagens database- og korpustilstand.
- Bekreftet S3-Cs kjente forbehold: generatorens `controlLinks.sourceRegistrationBatch` i alle 10 planlagte library-rader er hardkodet til v1-batchstien (`scripts/knowledge/generate-source-registration-plan.ts:1145` bruker `SOURCE_REGISTRATION_PATHS.batch`, ikke `--manifest`-argumentet). Dette er **ikke** korrigert — generatoren inngår i planens `inputBindings`/`codeBindings`, og en endring er en kontraktsrevisjon som krever eierbeslutning og ny gjennomgang.

## 4. Verifikasjon (kommando → resultat)

| Kommando | Resultat |
|---|---|
| `npm run knowledge:processing-contracts:check` | **282/282 pass**, 5 suiter, 0 fail |
| `npx tsc --noEmit` | **0 feil** |
| `node --import=tsx --test tests/lib/source-registration-trusted-entrypoint.test.ts` | **9/9 pass** |
| `node scripts/knowledge/launch-locked-source-registration-apply.mjs --apply` (ren env) | `APPLY_TRUSTED_ENTRYPOINT_REQUIRED`, exit 1 — karantene intakt |
| `node scripts/knowledge/verify-node-runtime-closure.mjs` | exit 0 |
| `node scripts/knowledge/verify-psql-runtime-closure.mjs` | exit 0 |
| `node scripts/knowledge/verify-postgresql-extension-runtime-closure.mjs` | `status:"pass"`, closureSha256 `3b5540bc…` |
| `npm run knowledge:corpus:check` | grønn: `active=1555; retained-history=17; missing-files=29; full-text-units=1467; owner-review=1555` |
| Generator `--dry-run` mot kandidatbatch | grønn, 10/10/0/0 — se §3.3 |
| `npm run knowledge:health:check` | **FEILER** — se §5 |

`git status --short` viser kun de tre pre-eksisterende ucommittete filene fra S3-C/S3-A (`source-registration-batch-2026-08-04-candidate.v1.json`, `source-registration-candidate-plan-2026-08-04.v1.json`, `corpus-role-classification-receipts.v1.jsonl`). Denne sesjonen la ikke til noe i repoet.

## 5. Gjenstår — eierstyrte beslutninger før signering

1. **Godkjenn eller forkast kandidatplanen** (`SESJON-3/RAPPORT-S3-C.md`). Teknisk verifisert fersk i dag; beslutningen er eiers.
2. **Avklar `controlLinks`-forbeholdet.** Enten aksepter at library-radenes kontroll-lenke peker på v1-batchstien, eller bestill en kontrollert generator-revisjon (ny `codeBindings`-gjennomgang). Anbefaling: revider generatoren til å bruke den faktiske batchstien — en hardkodet sti i en ellers pin-nøyaktig pipeline er en skjevhet som vil slå til igjen.
3. **Identifiser skriveren av database-driften.** 55 `SourceDoc` + 91 `SourceCitation` opprettet 2026-08-05 ~15:00 UTC, utenfor `ControlledMutationAudit`. Frem til dette er avklart er det uklart om driften er legitim arbeid (f.eks. en import i hovedrepoet) eller en prosess som burde vært gatet.
4. **Ny helse-vurdering.** `knowledge:health:check` feiler fordi live-tellingene har flyttet seg fra den immutable snapshoten (dette er *ikke* runtime-layout lenger — debug-diff viser at eneste avvik er DB-tellingene + avledede hasher). Krever ny assessment med `CORPUS_HEALTH_SUPERSEDES_ID` — en kontrollert operasjon, ikke gjort her.
5. **Ny kontrollert planrevisjon + signering.** Apply-verktøyet er hashbundet til den gamle planen i både kode og kontrakt (`SOURCE-REGISTRATION-APPLY-CONTRACT.md`). Å binde det til en ny plan er en kontraktsendring: ny uavhengig gjennomgang, ny before-snapshot/logisk tilstand (den gamle `77ac2b8f…` / 555 189 rader er uansett foreldet av punkt 3), deretter Ed25519-signering. Alt forberedende arbeid opp til denne beslutningen er gjort.
6. **Valgfritt:** slett det skadede `/private/tmp/food-systems-runtime.uecW2d` etter gjennomgang.

## 6. Forbehold

- Ingen `--apply`, `--rehearse-logical-clone`, signering eller databaseskriving er utført. Alle DB-kontroller kjørte i `REPEATABLE READ READ ONLY` eller via verktøyenes egne read-only-moduser.
- Private stier og `DATABASE_URL`-verdi er ikke skrevet til noen fil; de er maskert som `<privat-rot>` der de refereres.
- Runtime-lukningene (Node/psql/utvidelser) passerer med det nye treet, men apply-tidspunktets trusted launcher attesterer treet på nytt ved lansering — eventuell senere drift vil da stoppe fail-closed, som designet.
